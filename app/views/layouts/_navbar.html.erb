<div class="header">
  <div class="title"> Reviews </div>
  <% if current_user %>
    <div class="username">
        <i>Welcome, <%= current_user.username %></i>
    </div>
<% end %>
<div class="nav">
<p><div><a href="/products"> Products </a></div></p> 
<% if logged_in? %>
       <div><a href="/logout"> Log Out</a></div>
<% else %>
        <div><a href="/signup">Sign Up</a></div>
        <div><a href="/login">Log In</a></div> 
   <% end %>
   </div>
</div> 